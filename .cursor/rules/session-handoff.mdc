---
description: Resume Loyalty Lab from the last stopping point. Read this first in every new chat.
alwaysApply: true
---

# Resume here

Read `docs/08-session-handoff.md` before writing code. Then follow `docs/07-task-breakdown.md` in order.

**Next work:** **T-015** pricing HTTP (`GET /offers`, `POST /offers/{id}/quote`, `GET /quotes/{id}/explain`) plus the raw-JSON no-`netRate` assertion.

**Do not:** start F5 until Phases 0–5 are done; rewrite the design docs; implement features out of task order.

**Already implemented:** T-001–T-007 (Phase 0), T-010–T-014 (pricing rules, pipeline, traces, quotes, use cases with fake ports). Payment is out-of-process. Checkout is a saga (ADR-0006). Angular is Phase 4.
